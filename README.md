# The Longest Path — CSC 296

Live site: **https://liltoby123.github.io/assignment/**

A pictured walkthrough of the CSC 296 assignment:

1. **The 20-day project** — network diagram, forward/backward pass, critical path, slack
2. **The juice kitchen** — linear program, feasible region, corner-point optimum

GitHub Pages serves the static `index.html` at the repo root. The React source in `src/` is the same lesson, built for the Grok preview.
